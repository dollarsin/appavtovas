import 'package:flutter/cupertino.dart';

import '../../../passangers/ui/widgets/passenger_form.dart';

// TODO(lmepol): Implement tour reserve page
class TourReservePage extends StatelessWidget {
  const TourReservePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: PassengerForm());
  }
}
